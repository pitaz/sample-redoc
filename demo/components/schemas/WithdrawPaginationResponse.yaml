title: List of all withdraws with pagination
type: object
properties:
  withdraws:
    type: array
    description: withdraw response
    items:
      $ref: ./TenantWithdrawResponse.yaml
  next_token:
    type: integer
    description: Next token for pagination
description: List of withdraws and next_token
